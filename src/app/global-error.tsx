"use client";
import { useEffect } from "react";
export default function GlobalError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) { useEffect(() => { console.error("Global error boundary", error); }, [error]); return <html lang="ko"><body style={{ margin: 0, fontFamily: "Arial, Malgun Gothic, sans-serif", background: "#f4f7fa" }}><main style={{ minHeight: "100vh", display: "grid", placeContent: "center", textAlign: "center", padding: 24 }}><b style={{ color: "#c24b4b", fontSize: 12 }}>SYSTEM ERROR</b><h1 style={{ color: "#173650" }}>서비스를 표시하지 못했습니다</h1><p style={{ color: "#667085", fontSize: 13 }}>잠시 후 다시 시도해 주세요.</p>{error.digest && <small style={{ color: "#8794a0" }}>오류 참조: {error.digest}</small>}<button onClick={reset} style={{ margin: "18px auto", border: 0, borderRadius: 8, padding: "12px 18px", background: "#1976d2", color: "white", fontWeight: 700 }}>다시 시도</button></main></body></html>; }
